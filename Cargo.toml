[package]
name = "urlpattern"
version = "0.1.0"
edition = "2018"

[dependencies]
derive_more = "0.99.16"
url = "2.2.2"
regex = "1.5.4"
serde = { version = "1.0.127", features = ["derive"], optional = true }
serde_regex = { version = "1.1.0", optional = true }

[features]
serde_ = ["serde", "serde_regex"]
